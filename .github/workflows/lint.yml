name: Lint
on: [push]

jobs:
  lint:
    runs-on: ubuntu-20.04
    timeout-minutes: 30
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Run Checks
      run: |
        ./dev-setup.sh
        ./lint.sh
